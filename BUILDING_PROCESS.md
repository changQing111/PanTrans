# 泛基因组转录组构建过程详细说明

## 一、整体目标

该流程旨在构建**泛基因组转录组（Pan-genome Transcriptome）**，即整合多个品种（variety）的转录本数据，识别同源基因簇，并生成统一的、去冗余的转录本注释。这对于理解物种内的基因多样性、识别核心基因和可变基因具有重要意义。

---

## 二、详细构建流程

### 阶段一：数据准备与输入验证（第85-106行）

**目的**：确保输入数据的完整性和正确性

1. **输入方式选择**：
   - **方式A**：如果用户提供了已比对好的 BAM 文件（`aligned_bam_path`），则直接使用，此时 cDNA 和 gDNA 文件路径是可选的
   - **方式B**：如果没有提供 BAM 文件，则必须提供 cDNA FASTA 文件和 gDNA FASTA 文件

2. **元数据加载**：
   - 从 `variety_name_path` 文件读取所有品种的名称列表（每个品种占一行）
   - 从 `bed_path` 文件读取基因的位置信息（`bed_dic`）和链方向信息（`gene_strand_dic`）

---

### 阶段二：转录本与基因组比对（第108-124行）

**目的**：建立转录本与参考基因组基因之间的映射关系

#### 步骤2.1：执行序列比对

**情况A - 使用已有BAM文件**：
- 直接使用用户提供的比对结果
- 如果同时提供了 cDNA 文件，则从 cDNA FASTA 提取转录本长度
- 否则从 BAM 文件中的序列信息推断转录本长度

**情况B - 从头比对**：
- 使用 `minimap2` 工具将 cDNA 序列比对到 gDNA 序列
  - 参数说明：
    - `-ax splice:hq`：使用高质量剪接比对模式，适合 RNA-seq 数据
    - `-uf`：使用 forward strand，不输出反向链
    - `--secondary=yes`：保留次要比对结果
    - `-N 100`：最多输出100个次要比对
    - `--MD`：输出 MD 标签（用于计算错配）
    - `--cs=long`：输出长格式的剪接信息
    - `-t {threads}`：使用多线程加速
- 通过管道将 SAM 格式转换为 BAM 格式，并进行坐标排序
- 生成排序后的 BAM 文件：`{prefix}_cdna_align_gdna.bam`

#### 步骤2.2：过滤低质量比对

**目的**：只保留高质量的转录本-基因配对关系，为后续构建基因簇提供可靠基础

使用 `filter_bam()` 函数，基于以下三个质量指标进行严格筛选：

1. **覆盖度（Coverage）**：`cov = 匹配长度 / 转录本长度`，必须 >= 0.80
   - 确保转录本的大部分序列都能比对到基因上

2. **一致性（Identity）**：`pid = 1 - (错配数 / 对齐长度)`，必须 >= 0.90
   - 确保转录本与基因序列高度相似（90%以上一致）

3. **软截断比例（Soft-clipping ratio）**：`soft_ratio = 软截断长度 / 转录本长度`，必须 <= 0.10
   - 确保转录本两端未被比对的部分不超过10%

**重要**：这三个条件必须**同时满足**（`passed_criteria == 3`）才会保留该比对结果。

**输出结果**：
- 生成过滤后的 BAM 文件：`{prefix}_cdna_align_gdna.filtered.bam`
- 提取转录本-基因配对列表：`pair_gene_li`，格式为 `[(transcript_id, gene_id), ...]`
- 提取基因长度字典：`gene_len_dic`，用于后续聚类分析

**特殊处理**：
- 如果转录本 ID 和基因 ID 相同（`query_trans == refer_name`），直接记录配对关系，跳过质量检查
- 如果比对超出基因边界，则丢弃该比对

---

### 阶段三：构建同源基因关系图（第126-128行）

**目的**：将转录本-基因配对关系转化为图结构，以便识别同源基因簇

#### 步骤3.1：构建有向图

使用 `di_graph_from_pair(pair_gene_li)` 函数：
- 每个**基因**作为图中的一个**节点**
- 每个转录本-基因配对关系 `(transcript_id, gene_id)` 创建一条从转录本对应基因到目标基因的**有向边**
- 例如：如果转录本 T1 比对到基因 G1，则创建边 `G_source → G1`

**图的含义**：
- 如果两个基因之间相互有边（双向连接），说明它们可能被同一个或多个转录本比对到，很可能是同源基因
- 单向边可能表示部分比对或次要比对关系

#### 步骤3.2：识别强连通分量（SCC）

使用 `get_conn_comp(G)` 函数（基于 NetworkX 的 `strongly_connected_components`）：
- **强连通分量**定义：图中任意两个节点之间都存在有向路径的最大子图
- 每个 SCC 代表一组相互关联的基因，可能是同一个基因在不同品种中的变体，或是高度同源的基因家族成员

**输出**：`sccs`，一个包含多个 SCC 的列表，每个 SCC 是一个基因 ID 的集合

---

### 阶段四：递归分解与基因簇分配（第132行，调用 `assign_sccs()`）

**目的**：将复杂的强连通分量递归分解为最终的、合理的基因簇

这是整个流程中最复杂的部分，涉及多层次的处理策略：

#### 步骤4.1：初步分类处理

对于每个强连通分量（SCC）：

1. **单基因分量**（`len(li) == 1`）：
   - 直接作为最终的簇，无需进一步处理
   - 添加到 `pre_cluster` 和 `last_clusters`

2. **多基因分量**：
   - 进入递归分解流程

#### 步骤4.2：递归分解（`unit_recursion()` 函数）

**核心思想**：通过选择"代表基因"并识别双向连接来逐步分解复杂簇

**具体过程**：

1. **计算入度**：
   - 计算 SCC 内每个基因的入度（有多少其他基因指向它）
   - 入度高的基因通常是被更多转录本比对到的核心基因

2. **选择代表节点**：
   - 在入度最高的基因中，选择长度最长且在参考基因组中的基因作为代表（hub gene）
   - 代表节点将成为该簇的核心

3. **分离双向连接的基因**：
   - 将与代表节点**双向连接**（mutually connected）的基因归为一簇
   - 双向连接意味着两个基因之间存在相互比对关系，说明它们很可能是真正的同源关系
   - 单向连接的基因可能只是部分相似，需要单独处理

4. **递归处理剩余节点**：
   - 对于未能归入代表簇的节点，如果数量 > 2，则递归调用 `unit_recursion()` 继续分解
   - 如果数量 <= 2，则直接判断它们是否双向连接来决定是否合并

5. **输出初步簇**：
   - 将分解得到的簇添加到 `pre_cluster` 中

#### 步骤4.3：按染色体分配（`assign_gene_by_chrom()` 函数）

对于 `pre_cluster` 中的每个多基因簇（`len(cluster) > 1`）：

1. **染色体分组**：
   - 根据 BED 文件信息，将簇中的基因按所属染色体分组
   - 不同染色体上的基因不太可能是同一个基因，即使它们有比对关系

2. **分离染色体特异性簇**：
   - 如果一个簇包含来自多个染色体的基因，则将它们按染色体分开
   - 这样可以避免将不同染色体上的相似基因错误地归为一类

3. **再次递归分解**：
   - 对于分离后的每个染色体特异性簇，如果仍然包含多个基因，再次调用 `unit_recursion()` 进一步细化

#### 步骤4.4：生成最终簇列表

经过上述处理后，得到 `last_clusters`：
- 每个簇是一个基因 ID 列表
- 簇内基因被认为是同源的（orthologs）或高度相似的变体
- 不同簇代表不同的基因家族或功能单元

---

### 阶段五：输出基因簇结果（第133-146行）

**目的**：将聚类结果保存为文件，供后续分析和可视化使用

#### 步骤5.1：构建最终有向图

使用 `construct_di_graph_from_cluster(last_clusters)`：
- 对于每个簇，将第一个基因作为 hub（中心节点）
- 创建 hub 与其他基因之间的双向边
- 这样可以可视化簇内的基因关系

**输出**：`{prefix}_pan_genome_di_graph.txt`（有向图的边列表格式）

#### 步骤5.2：保存簇信息

1. **初步簇文件**（`{prefix}_pre.cluster`）：
   - 保存 `pre_cluster` 的结果
   - 每行一个簇，基因 ID 用制表符分隔
   - 反映递归分解后的初步结果

2. **最终簇文件**（`{prefix}_last.cluster`）：
   - 保存 `last_clusters` 的结果
   - 每行一个簇，基因 ID 用制表符分隔
   - 这是经过染色体分配和最终细化后的结果，可以直接用于后续分析

---

### 阶段六：转录本去冗余（第150-159行）

**目的**：基于剪接位点模式，去除每个基因簇内的冗余转录本，生成标准的 GTF 注释文件

#### 步骤6.1：转录本去重处理

调用 `transcript_dedup()` 函数，输入参数包括：

1. **`filter_bam_path`**：过滤后的比对文件，包含所有高质量的转录本-基因比对信息

2. **`cluster_dic`**：最终基因簇字典，用于确定哪些基因属于同一簇

3. **`trans_len_dic`**：转录本长度字典，用于评估转录本质量

4. **`gene_len_dic`**：基因长度字典，用于验证比对合理性

5. **`gene_strand_dic`**：基因链方向字典，确保转录本方向正确

#### 步骤6.2：去重策略（推断）

虽然具体实现细节在其他模块，但通常的去重逻辑可能包括：

1. **剪接位点比较**：
   - 对于映射到同一个基因簇的转录本，比较它们的剪接位点模式（splice junction pattern）
   - 剪接位点模式相同或高度相似的转录本被视为冗余

2. **转录本选择**：
   - 在每个冗余组中，可能选择最长的转录本、或覆盖度最高的转录本作为代表
   - 保留所有独特的剪接变体（alternative splicing isoforms）

3. **GTF 格式输出**：
   - 将去重后的转录本信息转换为标准的 GTF（Gene Transfer Format）格式
   - 包含基因位置、外显子边界、剪接位点等完整信息

**输出**：`{prefix}.gtf`，标准的转录本注释文件，可用于后续的基因表达分析和可视化

---

## 三、整体流程的生物学意义

### 3.1 为什么需要这个过程？

1. **多品种数据整合**：
   - 不同品种可能有相同基因的不同变体（alleles）
   - 需要识别哪些转录本对应的是同一个基因的不同形式

2. **同源基因识别**：
   - 通过比对关系和图分析，识别真正的同源基因（orthologs）
   - 区分真正的基因家族成员和由于序列相似性导致的误比对

3. **转录本复杂性处理**：
   - 同一基因可能有多个剪接变体
   - 需要去重以得到非冗余的转录本集合

### 3.2 关键算法思想

1. **图论方法**：
   - 将有向图用于建模基因间的相似性关系
   - 强连通分量识别用于发现紧密相关的基因群

2. **递归分解**：
   - 逐步细化复杂的基因簇
   - 通过双向连接判断确保同源关系的可靠性

3. **多层次验证**：
   - 质量过滤（比对质量）
   - 拓扑验证（图结构）
   - 位置验证（染色体分配）
   - 功能验证（剪接位点模式）

---

## 四、输出文件总结

| 文件名 | 内容 | 用途 |
|--------|------|------|
| `{prefix}_cdna_align_gdna.bam` | 排序后的原始比对文件 | 中间文件，可用于重新分析 |
| `{prefix}_cdna_align_gdna.filtered.bam` | 高质量比对文件 | 用于转录本去重和分析 |
| `{prefix}_pan_genome_di_graph.txt` | 基因关系有向图 | 可视化和关系分析 |
| `{prefix}_pre.cluster` | 初步基因簇 | 中间结果，反映分解过程 |
| `{prefix}_last.cluster` | 最终基因簇 | **核心输出**，用于后续分析 |
| `{prefix}.gtf` | 去重后的转录本注释 | **最终注释**，用于表达分析 |

---

## 五、注意事项

1. **参数调优**：
   - 比对质量阈值（COV_MIN, PID_MIN, SOFT_MAX）可根据数据质量调整
   - 过于严格可能丢失真实关系，过于宽松可能引入噪音

2. **计算资源**：
   - minimap2 比对和图的构建可能消耗大量内存和时间
   - 建议使用多线程（`threads` 参数）

3. **结果验证**：
   - 建议检查 `last.cluster` 文件，确认簇的大小和组成合理
   - 过大的簇可能需要手动检查是否有误聚类

4. **后续分析**：
   - 可以将 `last.cluster` 用于识别核心基因（所有品种都有的基因）
   - 可以识别可变基因（只在部分品种中出现的基因）
   - GTF 文件可用于差异表达分析和功能注释


